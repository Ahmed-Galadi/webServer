# Test Case 2: Index exists (TBI.html) + autoindex off
# Expected: 200 - Serves TBI.html (autoindex is ignored)

server {
    port 8080 ;
    host 127.0.0.1 ;
    server_name test_server ;
    client_max_body_size 10485760 ;

    location /cgi-bin/ {
        root www/cgi-bin/ ;
        index TBI.html ;
        autoindex off ;
        methods GET POST DELETE ;
    }
}
